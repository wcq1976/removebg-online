# 一键抠图 - MVP开发文档

---

## 一、产品定位

### 1.1 产品概述

| 项目 | 内容 |
|------|------|
| 产品名称 | 一键抠图 |
| 产品定位 | 免费在线AI图片背景移除工具 |
| 目标用户 | 微商、电商、普通人 |
| 核心价值 | 极简、免费、快速 |

### 1.2 关键亮点

- **纯内存处理** - 无存储成本，无隐私问题
- **5天开发周期** - 快速上线验证
- **低成本启动** - 月成本约$11
- **全球CDN加速** - Cloudflare部署

---

## 二、核心功能

### 2.1 功能列表

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 图片上传 | 支持点击/拖拽上传，限制10MB | P0 |
| AI抠图 | 调用Remove.bg API自动识别主体移除背景 | P0 |
| 结果预览 | 处理后可预览透明背景图 | P0 |
| 图片下载 | 一键下载PNG格式 | P0 |
| 重新处理 | 清空当前，返回上传状态 | P0 |

### 2.2 用户流程

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  打开    │ -> │  上传    │ -> │  AI处理  │ -> │  预览    │ -> 下载
│  页面    │    │  图片    │    │  3-5秒   │    │  结果    │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
```

### 2.3 异常处理

| 场景 | 处理方式 |
|------|----------|
| 文件太大 | 提示"图片不超过10MB" |
| 文件格式不对 | 提示"请上传图片文件" |
| API调用失败 | 提示"处理失败，请重试" |
| 网络超时 | 提示"网络异常，请检查网络" |

---

## 三、技术方案

### 3.1 技术栈

| 层级 | 技术选型 | 理由 |
|------|----------|------|
| 前端框架 | Next.js 14 | App Router, SSR/SSG |
| 部署平台 | Cloudflare Pages | 免费，全球CDN |
| AI抠图API | Remove.bg | 免费50张/月 |
| 图片处理 | 纯内存 | 无存储成本 |

### 3.2 架构设计

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  用户浏览器  │ ->  │  Next.js    │ ->  │ Remove.bg  │
│  (移动端)   │     │  (Cloudflare)│    │  (API)     │
└─────────────┘     └─────────────┘     └─────────────┘
                           │
                     ┌─────┴─────┐
                     │  纯内存    │
                     │  处理      │
                     │  无存储    │
                     └───────────┘
```

### 3.3 关键代码

#### API调用 (lib/removebg.ts)

```typescript
export async function removeBackground(imageBase64: string): Promise<Blob> {
  const formData = new FormData();
  formData.append('image_file', base64ToBlob(imageBase64));
  formData.append('size', 'auto');
  formData.append('format', 'png');

  const response = await fetch('https://api.remove.bg/v1.0/removebg', {
    method: 'POST',
    headers: {
      'X-Api-Key': process.env.REMOVE_BG_API_KEY!,
    },
    body: formData,
  });

  if (!response.ok) {
    throw new Error('Remove.bg API failed');
  }

  return response.blob();
}
```

#### 前端组件 (components/Upload.tsx)

```tsx
'use client';

import { useState, useCallback } from 'react';

export default function Upload() {
  const [file, setFile] = useState<File | null>(null);
  const [processing, setProcessing] = useState(false);
  const [result, setResult] = useState<string | null>(null);

  const handleDrop = useCallback((e: React.DragEvent) => {
    e.preventDefault();
    const dropped = e.dataTransfer.files[0];
    if (dropped?.type.startsWith('image/')) {
      setFile(dropped);
    }
  }, []);

  const process = async () => {
    if (!file) return;
    setProcessing(true);
    // 调用API处理
    const blob = await removeBackground(await fileToBase64(file));
    setResult(URL.createObjectURL(blob));
    setProcessing(false);
  };

  return (
    <div onDrop={handleDrop} className="upload-card">
      {result ? (
        <img src={result} alt="Result" />
      ) : (
        <>
          <input type="file" onChange={e => setFile(e.target.files?.[0])} />
          <button onClick={process} disabled={!file || processing}>
            {processing ? '处理中...' : '开始处理'}
          </button>
        </>
      )}
    </div>
  );
}
```

---

## 四、界面设计

### 4.1 页面布局

```
┌────────────────────────────────────────┐
│  Header                                 │
│  [Logo: ✂️ 一键抠图]     [Badge: 免费] │
├────────────────────────────────────────┤
│                                         │
│  Hero区域                               │
│  AI智能一键抠图                         │
│  上传图片，3秒自动移除背景               │
│                                         │
├────────────────────────────────────────┤
│                                         │
│  上传卡片 (核心交互区)                  │
│  ┌──────────────────────────────────┐  │
│  │ 📤 点击或拖拽图片到这里          │  │
│  │ 支持 JPG、PNG、WebP，最大10MB    │  │
│  └──────────────────────────────────┘  │
│                                         │
│  [ 🔄 开始处理 ]                        │
│                                         │
├────────────────────────────────────────┤
│  处理状态 (Processing)                   │
│  ┌──────────────────────────────────┐  │
│  │    ⚡ AI正在智能抠图...          │  │
│  │    ████████████░░░░ 70%         │  │
│  └──────────────────────────────────┘  │
│                                         │
├────────────────────────────────────────┤
│  结果展示 (Result)                      │
│  ┌──────────────────────────────────┐  │
│  │         [处理结果图片]            │  │
│  └──────────────────────────────────┘  │
│  [🔄 重新处理]  [💾 下载PNG]          │
│                                         │
├────────────────────────────────────────┤
│  Features                               │
│  ⚡3秒出结果  🎯发丝级精度  💯完全免费 │
│                                         │
├────────────────────────────────────────┤
│  Footer © 2026                         │
└────────────────────────────────────────┘
```

### 4.2 移动端适配

| 屏幕尺寸 | 布局 |
|----------|------|
| < 640px | 单列，100%宽度 |
| 640-1024px | 单列，最大600px居中 |
| > 1024px | 单列，最大600px居中 |

### 4.3 设计规范

| 元素 | 规范 |
|------|------|
| 主色调 | #6366f1 (Indigo) |
| 渐变 | linear-gradient(135deg, #6366f1, #8b5cf6) |
| 圆角 | 12px (卡片), 8px (按钮) |
| 字体 | Noto Sans SC |
| 阴影 | 0 4px 6px rgba(0,0,0,0.1) |

---

## 五、开发计划

### 5.1 里程碑 (5天)

| 天数 | 任务 | 交付物 |
|------|------|--------|
| Day 1 | 项目初始化 + UI框架搭建 | Next.js项目，基础布局 |
| Day 2 | 上传组件 + API集成 | 图片上传功能 |
| Day 3 | 处理流程 + 结果展示 | 完整处理流程 |
| Day 4 | 响应式优化 + 动画 | 移动端适配 |
| Day 5 | 测试 + 部署上线 | 上线可用 |

### 5.2 每日目标

- **Day 1**: `npm create next-app@latest` + 基础页面结构
- **Day 2**: 实现拖拽上传 + Remove.bg API集成
- **Day 3**: 处理状态 + 结果展示 + 下载
- **Day 4**: CSS优化 + Loading动画 + 移动端适配
- **Day 5**: Cloudflare Pages部署 + 测试验证

---

## 六、成本预估

### 6.1 开发期成本

| 项目 | 成本 | 备注 |
|------|------|------|
| 域名 | $0 | 使用Cloudflare默认域名 |
| 托管 | $0 | Cloudflare Pages免费 |
| API | $0 | Remove.bg免费50张/月 |
| **合计** | **$0** | |

### 6.2 运营期成本 (月)

| 项目 | 成本 | 备注 |
|------|------|------|
| 域名 | $0 | .workers.dev免费子域名 |
| 托管 | $0 | Cloudflare Pages免费 |
| API | $11-214 | 超出免费额度后 |
| **合计** | **$11-214** | 取决于用量 |

### 6.3 成本优化策略

1. **免费额度内**: 50张/月，满足初期需求
2. **用户付费**: 超过免费次数引导付费
3. **广告收入**: 免费用户看广告补贴API成本

---

## 七、商业化策略

### 7.1 版本设计

| 版本 | 价格 | 功能 |
|------|------|------|
| 免费版 | ¥0 | 每天3次，压缩画质 |
| Pro版 | ¥9.9/月 | 无限次，高清原图 |
| 企业版 | 定制 | API调用，定制功能 |

### 7.2 变现路径

```
第1阶段: 免费上线 -> 积累用户
    ↓
第2阶段: 达到免费限额用户引导付费
    ↓
第3阶段: 广告+付费会员双收入
```

### 7.3 定价策略

- **免费版**: 引流+口碑
- **Pro版**: ¥9.9/月 = 一天3毛，性价比高
- **企业版**: 按需报价

---

## 八、技术风险

### 8.1 主要风险

| 风险 | 影响 | 应对方案 |
|------|------|----------|
| API免费额度用完 | 服务不可用 | 1. 提前通知用户 2. 引导付费 3. 备用API |
| 图片处理超时 | 用户体验差 | 1. 超时提示 2. 重试机制 3. 加载动画 |
| 移动端兼容性 | 部分功能不可用 | 1. 渐进增强 2. 多浏览器测试 |
| 恶意请求 | API额度被耗尽 | 1. IP限流 2. 验证码 3. 登录门槛 |

### 8.2 监控告警

- API调用次数 > 80% 告警
- 错误率 > 10% 告警
- 响应时间 > 10s 告警

---

## 九、验收标准

### 9.1 功能验收

- [ ] 支持点击上传图片
- [ ] 支持拖拽上传图片
- [ ] AI抠图成功
- [ ] 结果预览正常
- [ ] 下载功能正常
- [ ] 重新处理功能正常

### 9.2 性能验收

| 指标 | 目标 |
|------|------|
| 页面加载 | < 1秒 |
| API响应 | < 5秒 |
| 首次输入反馈 | < 100ms |

### 9.3 体验验收

- [ ] 移动端正常显示
- [ ] Loading动画流畅
- [ ] 错误提示友好
- [ ] 整体视觉精美

---

## 十、下一步

1. ✅ 需求文档确认
2. ⏳ 代码开发 (5天)
3. ⏳ 测试验证
4. ⏳ 部署上线
5. ⏳ 运营迭代

---

*文档版本：v1.0*
*创建时间：2026-03-20*
*作者：吴超群*
